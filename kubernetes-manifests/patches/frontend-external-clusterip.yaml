# Generated by DuploCloud kube-deploy.
# Neutralizes the LoadBalancer Service so the ALB Ingress is the sole external exposure.
apiVersion: v1
kind: Service
metadata:
  name: frontend-external
  namespace: mircoservices-app
spec:
  type: ClusterIP
