# Generated by DuploCloud kube-deploy.
# Neutralizes the frontend-external LoadBalancer Service so only the shared ALB Ingress
# provides external access. An unmanaged second LB would create an uncontrolled exposure.
apiVersion: v1
kind: Service
metadata:
  name: frontend-external
  namespace: microservices-demo-main
spec:
  type: ClusterIP
